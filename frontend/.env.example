# Frontend environment variables
# These are optional; the app auto-detects API URL from the current host

# For development with custom API server (if needed):
# VITE_API_URL=http://192.168.0.100:3000/api
